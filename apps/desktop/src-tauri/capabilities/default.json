{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for Coinbox",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "stronghold:default"
  ]
}
